# -*- mode: snippet -*-
# name: equality operator
# --
inline std::ostream& operator<<(std::ostream& stream, const $1& value) {
    stream << "\"$1\": {"
           << $0
           << " }";

    return(stream);
}
