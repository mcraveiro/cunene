# vim: set sw=4: -*- mode: snippet; tab-width: 4; indent-tabs-mode: nil -*- */
#name : if (...) { ... } else if (...) { ... } else { ... }
# --
if (${1:condition}) {
    $2
} else if (${3:condition}) {
    $4
} else {
    $0
}        
